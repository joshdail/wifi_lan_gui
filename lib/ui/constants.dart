import 'package:flutter/material.dart';

class UIConstants {
  // Text Constants
  static const String appTitle = 'Wifi LAN Manager';
  static const String scanButton = 'Scan Network';
  static const String unknownVendor = 'Unknown Vendor';

  // Colors
  static const Color errorColor = Colors.redAccent;

  // Padding
  static const double screenPadding = 16.0;
  static const double height = 16.0;

  // Text Styles
  static const TextStyle errorTextStyle = TextStyle(color: errorColor);
}
